# 개인 프로젝트 간략 PDF 페이지 구성안

이 문서는 머신러닝, 딥러닝, LLM/RAG 개인 프로젝트를 각각 4~5페이지짜리 포트폴리오 PDF로 다시 만들기 위한 페이지별 구성안입니다.

## 전체 방향

- 각 PDF는 `프로젝트 이해 -> 데이터/구조 -> 구현 -> 결과 -> 트러블슈팅/개선` 흐름으로 구성한다.
- 요약본은 기존 산출물을 없애거나 축소 평가하는 문서가 아니라, 긴 내용을 채용용 흐름으로 압축하고 재배치하는 문서다.
- 긴 산출물의 내용은 원본 PDF에 남기고, 요약본에는 채용 담당자나 면접관이 2~3분 안에 이해할 핵심만 배치한다.
- 코드 캡처는 프로젝트마다 2~3개만 사용한다.
- 한 페이지에는 큰 캡처를 최대 2개만 사용한다.
- 캡처 후보는 전부 넣거나 버리라는 뜻이 아니라, 페이지 목적에 맞게 `넣기`, `문장 요약`, `다른 페이지 이동` 중 하나로 처리하는 재료다.
- 화면 캡처는 가능하면 실제 Streamlit 실행 화면을 사용한다.
- 페이지 하단에는 `GitHub`, `Demo`, `기존 산출물 PDF` 링크를 작게 배치한다.

## 디자인 방향

디자인은 완전히 다르게 만들기보다 같은 템플릿을 쓰는 것이 좋다. 세 PDF가 같은 사람이 만든 포트폴리오라는 인상을 주기 때문이다.

다만 프로젝트 성격이 다르므로 아래 정도만 다르게 준다.

- 공통: 같은 폰트, 같은 여백, 같은 제목 위치, 같은 하단 링크 영역
- 머신러닝: 표, 지표, 비교 중심. 차분한 blue/teal 계열 포인트
- 딥러닝: 이미지 샘플, 예측 결과, 혼동행렬 중심. coral/gold 계열 포인트
- LLM/RAG: 처리 흐름, 검색 근거, fallback 구조 중심. teal/indigo 계열 포인트

즉, `같은 디자인 시스템 + 프로젝트별 포인트 색상/대표 시각자료만 다르게` 가 가장 좋다.

## 페이지 배치 원칙

앞으로 각 페이지는 아래 원칙으로 만든다.

- 페이지마다 메시지는 1개만 둔다.
- 텍스트 박스는 2~3개까지 허용한다.
- 큰 시각자료는 1개가 가장 좋고, 필요한 경우 2개까지 사용한다.
- 코드 캡처는 읽히는 크기로 넣을 때만 사용한다.
- 코드 캡처 2개와 그래프 1개를 같은 크기로 나란히 넣지 않는다.
- 캡처가 3개 이상 필요하면 한 페이지에 모두 밀어 넣지 않고 다음 페이지, 부록, 또는 문장 요약으로 나눈다.
- `필수 자료`는 반드시 넣고, `보조 자료`는 공간과 메시지에 맞게 넣는다.

## 캡처 선택 기준

- 필수 자료: 페이지 메시지를 증명하는 대표 자료. 캡처, 그래프, 표, 흐름도 중 하나가 될 수 있다.
- 보조 자료: 설명을 보강하는 자료. 공간이 부족하면 문장으로 요약하거나 다음 페이지로 이동한다.
- 이동/요약 자료: 너무 작게 넣으면 읽히지 않거나 다른 자료와 역할이 겹치는 자료. 빠지는 것이 아니라 위치를 바꾸거나 문장으로 압축한다.

예시:

- 데이터 구성 페이지는 `데이터 흐름 + 분포 그래프`가 우선이다.
- 모델 구현 페이지는 `모델 정의 코드 + 학습 설정`이 우선이다.
- 시연 페이지는 `실제 화면 + 결과 해석`이 우선이다.
- 트러블슈팅 페이지는 오류 화면 캡처가 없으면 코드 캡처 대신 `문제-원인-해결` 카드로 요약한다.

## 최종 권장 분량

각 프로젝트는 5페이지 구성을 권장한다.

1. 표지/요약
2. 문제 정의와 데이터/구조
3. 구현 핵심
4. 결과와 시연 화면
5. 트러블슈팅과 개선 방향

---

## 1. 머신러닝 간략 PDF

프로젝트명: 학생 생활패턴 기반 최종 성적 예측 및 학업 위험군 분류

권장 분량: 5페이지

### Page 1 - 프로젝트 요약

목표:
프로젝트 첫 장에서 `무엇을 만들었는지`를 빠르게 보여준다. 문제 정의는 Page 2에서 자세히 다루고, Page 1은 핵심 기능과 대표 결과 중심으로 구성한다.

상단 제목:
`학생 생활패턴 기반 성적 예측 및 위험군 분류`

한 줄 소개:
`UCI Student Performance Dataset을 활용해 회귀 모델로 최종 성적을 예측하고, 분류 모델로 위험/보통/우수 그룹을 판정한 머신러닝 프로젝트입니다.`

왼쪽 영역 - 핵심 기능:
- 학생 생활패턴과 학교생활 정보를 활용한 최종 성적 예측
- G3 점수 기준 위험/보통/우수 학업 수준 분류
- Model A/B 비교를 통한 입력 변수 영향 분석
- Streamlit 기반 예측 화면 구현

중앙 영역 - 대표 결과:
- 회귀 최고 모델: LinearRegression_B
- RMSE: 1.215
- R2: 0.849
- 분류 최고 모델: LogisticRegression_B
- F1-score: 0.869

오른쪽 영역 - 사용 기술:
- Python, Pandas, Scikit-learn
- Linear Regression, KNN Regression
- Logistic Regression, KNN Classifier, SVM
- Streamlit

캡처 후보:
- 필수 자료: Streamlit 최종 예측 화면 1장
- 보조 자료: 성능 비교표 또는 대표 그래프 1장
- 이동/요약 자료: 자세한 데이터 구성과 Model A/B 설계는 Page 2에서 사용

넣을 개념 박스:
- 회귀: 최종 성적 점수 예측
- 분류: 위험/보통/우수 그룹 판정
- 평가지표: RMSE, R2, Accuracy, F1-score

### Page 2 - 문제 정의와 데이터 설계

목표:
왜 이 프로젝트를 했고, 어떤 데이터를 어떤 기준으로 나누었는지 보여준다.

상단 제목:
`문제 정의와 데이터 구성`

왼쪽 영역 - 문제 정의:
- 최종 성적이 나온 뒤에는 학업 위험군 대응이 늦다.
- 생활패턴, 결석, 가정환경, 과거 성적을 활용하면 사전 예측 가능성을 확인할 수 있다.
- 예측 결과를 화면으로 확인할 수 있도록 Streamlit 프로토타입까지 구현했다.

중앙 영역 - 데이터 정보:
- 데이터: UCI Student Performance Dataset
- 전체 규모: 649행, 33개 컬럼
- 회귀 target: G3 최종 성적
- 분류 target: G3 기준 위험/보통/우수 그룹
- 위험: 0~9점
- 보통: 10~14점
- 우수: 15~20점

오른쪽 영역 - Model A/B 설계:
- Model A: 생활패턴, 학교생활, 가정환경, 결석 정보
- Model B: Model A + G1, G2 과거 성적
- 비교 목적: 과거 성적 포함 여부가 최종 성적 예측에 미치는 영향 확인

캡처 후보:
- 데이터 기본 확인 코드
- 학업 수준 그룹 생성 코드
- Model A/B 변수 분리 코드

### Page 3 - EDA와 모델 구현

목표:
데이터를 어떻게 확인하고 어떤 모델을 학습했는지 보여준다.

상단 제목:
`EDA와 모델 학습 흐름`

왼쪽 영역 - EDA 핵심:
- G1, G2는 G3와 높은 상관관계를 보였다.
- studytime은 약한 양의 관계, failures는 음의 관계를 보였다.
- 결측치와 중복값은 없어 별도 제거 없이 전처리를 진행했다.

중앙 영역 - 모델 구성:
- 회귀: Linear Regression, KNN Regression
- 분류: Logistic Regression, KNN Classifier, SVM
- 전처리: 범주형 변수 인코딩, 학습/테스트 데이터 분리, 일부 모델 스케일링

오른쪽 영역 - 분석 흐름:
`데이터 확인 -> EDA -> 인코딩 -> train/test split -> 모델 학습 -> 지표 평가 -> 앱 시연`

캡처 후보:
- 상관관계 Heatmap
- G1/G2/G3 관계 그래프
- 모델 학습 코드 일부

### Page 4 - 성능 결과와 시연 화면

목표:
모델 성능 결과와 내가 판단한 개선 포인트를 보여준다.

상단 핵심 결과:
- 회귀 최고 모델: `LinearRegression_B`
- RMSE: `1.215`
- R2: `0.849`
- 분류 최고 모델: `LogisticRegression_B`
- Accuracy: `0.869`
- F1-score: `0.869`

왼쪽 영역 - 성능 비교 표:
작은 표로 다음만 남긴다.

| 구분 | 최고 모델 | 핵심 지표 | 해석 |
| --- | --- | --- | --- |
| 회귀 | LinearRegression_B | RMSE 1.215 / R2 0.849 | 과거 성적 포함 시 예측력 상승 |
| 분류 | LogisticRegression_B | F1 0.869 | 위험군 분류 성능 가장 우수 |

가운데 영역 - Streamlit 화면:
- 입력값 요약 화면
- 예측 결과 카드
- 모델 비교 탭

오른쪽 영역 - 결과 해석:
- 생활패턴만 사용한 Model A보다 G1, G2를 포함한 Model B가 전반적으로 성능이 높았다.
- 최종 성적 G3는 과거 성적 G1, G2와 높은 관련성을 가진다.
- 생활패턴 변수만으로는 성적을 충분히 설명하는 데 한계가 있었다.

코드 캡처 후보:
- `default_regression_result()`
- `default_classification_result()`
- 예측 결과 출력 코드

### Page 5 - 트러블슈팅과 개선 방향

목표:
단순 구현 결과가 아니라 문제를 어떻게 해석하고 해결했는지 보여준다.

왼쪽 영역 - 트러블슈팅:
1. 범주형 데이터 처리
   - 문제: school, sex, address 등 문자형 컬럼은 모델이 바로 학습할 수 없음
   - 해결: 인코딩으로 숫자형 데이터로 변환
2. 모델별 전처리 차이
   - 문제: KNN, SVM은 변수 스케일에 민감함
   - 해결: 스케일링을 적용해 모델 비교 기준을 맞춤
3. 변수 구성에 따른 성능 차이
   - 문제: 생활패턴만으로는 G3 설명력이 낮음
   - 해결: Model A/B를 분리해 G1, G2가 성능에 미치는 영향을 비교

오른쪽 영역 - 개선 방향:
- 데이터 불균형 여부와 그룹 기준을 더 세밀하게 검토
- RandomForest, XGBoost 등 트리 기반 모델 추가 비교
- 중요 변수 해석을 위해 feature importance 또는 SHAP 적용
- 실제 서비스라면 민감 정보와 윤리적 활용 기준 필요

하단 문구:
`이 프로젝트를 통해 회귀와 분류 문제를 같은 데이터셋에서 비교하고, 입력 변수 구성과 전처리 방식이 모델 성능에 미치는 영향을 확인했습니다.`

---

## 2. 딥러닝 간략 PDF

프로젝트명: CNN 기반 가구 이미지 카테고리 분류

권장 분량: 5페이지

### Page 1 - 프로젝트 요약

목표:
프로젝트 첫 장에서 `무엇을 만들었는지`를 빠르게 보여준다. 문제 정의와 데이터 세부 설명은 Page 2로 보내고, Page 1은 핵심 기능과 대표 결과 중심으로 구성한다.

상단 제목:
`CNN 기반 가구 이미지 카테고리 분류`

한 줄 소개:
`CIFAR-100에서 가구 관련 5개 클래스를 추출해 CNN 모델을 학습하고, 업로드 이미지의 가구 카테고리를 예측하는 딥러닝 프로젝트입니다.`

왼쪽 영역 - 핵심 기능:
- CIFAR-100에서 가구 관련 5개 클래스 추출
- CNN 모델을 활용한 가구 이미지 카테고리 분류
- 업로드 이미지 기반 예측 결과와 Top-3 후보 출력
- Streamlit 기반 시연 화면 구현

중앙 영역 - 대표 결과:
- 테스트 정확도: 63.2%
- 테스트 데이터: 500장
- 분류 클래스: 5개
- 상대적으로 잘 분류한 클래스: chair, wardrobe
- 혼동이 있었던 클래스: bed, couch

오른쪽 영역 - 사용 기술:
- Python, TensorFlow/Keras
- CNN, Conv2D, MaxPooling2D
- Matplotlib, Pandas
- Streamlit

넣을 개념 박스:
- Conv2D: 이미지의 윤곽, 색상, 형태 특징 추출
- MaxPooling: 중요한 특징을 압축
- Dense: 추출된 특징으로 최종 카테고리 분류

캡처 후보:
- 필수 자료: Streamlit 예측 화면 1장
- 보조 자료: 가구 샘플 이미지 또는 학습 결과 그래프 1장
- 이동/요약 자료: 자세한 데이터 구성과 CNN 구조는 Page 2, Page 3에서 사용

### Page 2 - 문제 정의와 데이터 구성

목표:
이미지 분류가 왜 필요한지, 어떤 데이터를 사용했는지 보여준다.

상단 제목:
`문제 정의와 데이터 구성`

왼쪽 영역 - 문제 정의:
- 텍스트 기반 검색은 가구의 형태, 색상, 구조를 충분히 반영하기 어렵다.
- 이미지에서 직접 시각적 특징을 추출하면 카테고리 자동 분류로 확장할 수 있다.
- 향후 상품 등록, 이미지 검색, 추천 기능과 연결 가능하다.

중앙 영역 - 데이터 구성:
- 데이터셋: CIFAR-100
- 전체 클래스: 100개
- 사용 클래스: bed, chair, couch, table, wardrobe
- 학습 데이터: 2,500장
- 테스트 데이터: 500장
- 이미지 크기: 32 x 32 RGB

오른쪽 영역 - 전처리:
- 가구 관련 5개 클래스만 필터링
- 기존 라벨을 0~4로 재매핑
- 픽셀값을 0~1 범위로 정규화
- 학습/검증/테스트 흐름 구성

캡처 후보:
- 필수 자료: 클래스별 데이터 분포 그래프
- 보조 자료: CIFAR-100 가구 클래스 필터링 코드
- 이동/요약 자료: 라벨 변환 코드는 Page 3 구현 흐름으로 이동하거나 문장으로 요약

권장 배치:
- 상단: 문제 정의와 데이터 구성 요약
- 중간: `CIFAR-100 -> 5개 클래스 필터링 -> 라벨 0~4 재매핑 -> 정규화 -> 학습/검증/테스트` 흐름
- 하단 왼쪽: 필터링 코드 1개
- 하단 오른쪽: 클래스별 데이터 분포 그래프 크게

주의:
- 이 페이지에 코드 캡처 2개와 그래프 1개를 모두 같은 크기로 넣지 않는다.
- 라벨 변환은 빠지는 것이 아니라 흐름도 문장 또는 Page 3의 구현 설명으로 재배치한다.

### Page 3 - CNN 모델 구현

목표:
CNN 구조와 학습 과정을 보여준다.

상단 제목:
`CNN 모델 정의와 학습`

왼쪽 영역 - CNN 구조:
`Conv2D -> MaxPooling2D -> Conv2D -> MaxPooling2D -> Flatten -> Dense -> Output`

중앙 영역 - 개념 설명:
- Conv2D: 윤곽, 색상, 형태 특징 추출
- MaxPooling: 주요 특징을 압축해 계산량 감소
- Flatten/Dense: 추출된 특징을 바탕으로 최종 클래스 분류
- Adam optimizer와 sparse categorical crossentropy 사용

오른쪽 영역 - 학습 방식:
- 전처리된 이미지 데이터로 CNN 모델 학습
- 검증 데이터를 통해 accuracy/loss 변화 확인
- 학습 완료 모델을 저장해 Streamlit 앱에서 재사용

캡처 후보:
- 필수 자료: CNN 모델 정의 코드, `train_model.ipynb`의 `Sequential`, `Conv2D`, `MaxPooling2D`, `Flatten`, `Dense` 셀
- 보조 자료: model.compile 코드, `train_model.ipynb`의 Adam, sparse_categorical_crossentropy 설정 셀
- 보조 자료: model.fit 코드, `train_model.ipynb`의 epochs, batch_size, validation_split 학습 셀
- Page 4 이동 권장: 학습 정확도/loss 그래프

권장 배치:
- 왼쪽: CNN 구조를 한 줄 흐름으로 설명
- 중앙: CNN 모델 정의 코드 크게
- 오른쪽: optimizer/loss/epochs/batch_size/validation_split을 작은 설정 요약표로 정리

주의:
- `furniture_app.py`의 `load_model()` 코드는 CNN 모델 정의가 아니라 저장된 모델을 앱에서 재사용하는 코드다.
- 따라서 Page 3의 모델 구현 캡처에는 `train_model.ipynb`를 사용하고, Page 4의 Streamlit 시연/재사용 구조 캡처에는 `furniture_app.py`를 사용한다.

### Page 4 - 성능 결과와 시연 화면

목표:
모델 성능과 실제 시연 화면, 한계와 개선 방향을 함께 보여준다.

상단 핵심 결과:
- 테스트 정확도: `63.2%`
- 테스트 데이터: `500장`
- 분류 클래스: `5개`
- 상대적으로 잘 분류한 클래스: `chair`, `wardrobe`
- 혼동이 있었던 클래스: `bed`, `couch`

왼쪽 영역 - 결과 해석:
- 32 x 32 저해상도 이미지라 실제 가구 형태가 충분히 표현되지 않는다.
- chair와 wardrobe는 형태적 특징이 비교적 뚜렷해 성능이 높았다.
- bed와 couch는 형태가 유사해 혼동되는 경우가 있었다.

가운데 영역 - Streamlit 화면:
- 이미지 업로드 화면
- 예측 결과 카드
- Top-3 예측 후보
- 클래스별 예측 확률 그래프

오른쪽 영역 - 시연/재사용 메모:
이 페이지는 트러블슈팅 페이지가 아니라 성능과 시연 구조를 보여주는 페이지다.

- 학습한 모델은 `models/furniture_cnn.keras`로 저장했다.
- Streamlit 앱에서는 모델을 다시 학습하지 않고 `load_model()`로 불러와 예측에 사용했다.
- 업로드 이미지는 32 x 32 RGB로 변환한 뒤 예측한다.
- 예측 결과는 카테고리, confidence, Top-3 후보로 시각화한다.

캡처 기준:
- 필수 자료: Streamlit 예측 결과 화면 또는 confusion matrix 중 1개
- 보조 자료: 학습 accuracy/loss 그래프
- 이동/요약 자료: Streamlit 모델 로드 코드는 이 페이지 하단의 작은 구현 메모로 처리할 수 있다.

### Page 5 - 트러블슈팅, 개선 방향과 소감

목표:
기존 산출물의 트러블슈팅을 문장 카드로 요약하며, 모델의 한계와 다음 개선 방향을 함께 정리한다.

왼쪽 영역 - 트러블슈팅 요약:
1. Jupyter Notebook TensorFlow 인식 오류
   - 원인: 터미널 가상환경과 Jupyter 커널의 Python 환경이 달랐다.
   - 해결: Jupyter 커널을 `.venv` Python 환경으로 변경했다.
2. Streamlit 실행 시 TensorFlow 없음 오류
   - 원인: 가상환경이 활성화되지 않은 상태에서 앱을 실행했다.
   - 해결: `.venv` 활성화 후 `streamlit run furniture_app.py`로 실행했다.
3. CPU 학습 시간 과다
   - 원인: CNN 이미지 학습은 CPU 환경에서 시간이 오래 걸렸다.
   - 해결: 향후 Colab 또는 Kaggle GPU 환경 활용을 개선 방향으로 정리했다.

중앙 영역 - 개선 방향:
- 실제 쇼핑몰 가구 이미지 데이터셋으로 재학습
- 데이터 증강 적용
- MobileNet, EfficientNet 등 전이학습 모델 적용
- 예측 결과를 상품 카테고리 자동 추천 기능으로 연결

오른쪽 영역 - 한계 인식과 서비스 확장:
- CIFAR-100은 32 x 32 저해상도라 실제 쇼핑몰 이미지와 차이가 있다.
- 기본 CNN 구조는 복잡한 상품 이미지 분류에 한계가 있다.
- bed와 couch처럼 형태가 유사한 클래스는 혼동이 발생할 수 있다.
- 실제 서비스에서는 데이터 품질, 이미지 해상도, 촬영 각도, 배경 차이를 함께 고려해야 한다.
- 상품 등록 시 이미지 기반 카테고리 자동 추천
- 이미지 기반 검색 기능과 연결
- PlusHome 상품 추천 흐름과 연동
- LLM/RAG 프로젝트의 이미지 입력 단계로 재사용

캡처 기준:
- 이 페이지의 필수 자료는 캡처가 아니라 `문제-원인-해결` 트러블슈팅 카드다.
- 캡처를 넣고 싶다면 `학습 accuracy/loss 그래프`, `confusion matrix`, `Streamlit 예측 화면` 중 아직 앞 페이지에 쓰지 않은 1개를 사용한다.
- `load_model()` 코드는 트러블슈팅 근거가 아니라 저장 모델 재사용 구조이므로, Page 4 또는 작은 구현 메모로만 사용한다.

하단 문구:
`이 프로젝트를 통해 이미지 데이터 전처리, CNN 모델 학습, 예측 결과 시각화, Streamlit 기반 시연 화면 구현까지 딥러닝 분류 흐름을 경험했습니다.`

---

## 3. LLM/RAG 간략 PDF

프로젝트명: 이미지 분류와 RAG 기반 가구 추천 챗봇

권장 분량: 5페이지

### LLM/RAG 캡처 위치 상세

캡처는 아래 자료를 기준으로 사용한다.

- 기존 산출물 PDF: `F:\인공지능 심화과정\내 프로젝트\개인프로젝트\LLM\딥러닝+LLM 산출물(모하영).pdf`
- 앱 코드: `F:\인공지능 심화과정\내 프로젝트\개인프로젝트\LLM\소스\app.py`
- RAG 코드: `F:\인공지능 심화과정\내 프로젝트\개인프로젝트\LLM\소스\rag_core.py`
- 이미지 분류 코드: `F:\인공지능 심화과정\내 프로젝트\개인프로젝트\LLM\소스\image_predictor.py`
- RAG 지식 문서: `F:\인공지능 심화과정\내 프로젝트\개인프로젝트\LLM\데이터모델\furniture_knowledge.md`

전체 처리 구조도는 다음 중 하나로 처리한다.

- 가장 빠른 방법: 기존 산출물 PDF 7페이지의 `시스템 전체 구조` 슬라이드를 캡처한다.
- 권장 방법: 기존 PDF 7페이지를 참고하되, 현재 요약 PDF 템플릿에 맞춰 직접 다시 그린다.
- 직접 그릴 때 흐름: `이미지 업로드 -> 이미지 전처리 -> MobileNetV2 이미지 분류 -> 카테고리 추출 -> RAG 검색 -> LLM 또는 fallback 답변 생성 -> Streamlit 결과 출력`

주의:
- 기존 PDF 7페이지를 그대로 캡처하면 빠르지만, 새 요약본 디자인과 톤이 다를 수 있다.
- 디자인 통일을 우선하면 구조도는 PPT에서 도형으로 다시 그리는 것이 좋다.
- 외장하드의 현재 `rag_core.py` 캡처는 `Ollama -> RAG fallback` 흐름 중심이다. Gemini/Groq 내용은 기존 산출물과 설명 문구에는 남길 수 있지만, 코드 캡처는 실제 소스에 보이는 흐름만 사용한다.

### Page 1 - 프로젝트 요약

목표:
프로젝트 첫 장에서 `무엇을 만들었는지`를 빠르게 보여준다. 문제 정의를 길게 풀지 않고, 핵심 기능과 결과 화면 중심으로 구성한다.

상단 제목:
`이미지 분류와 RAG 기반 가구 추천 챗봇`

한 줄 소개:
`사용자가 업로드한 가구 이미지를 분류하고, 예측된 카테고리와 질문을 바탕으로 관련 문서를 검색해 추천 답변을 생성하는 LLM/RAG 프로토타입입니다.`

왼쪽 영역 - 핵심 기능:
- 이미지 업로드 기반 가구 카테고리 분류
- 예측 카테고리와 사용자 질문을 함께 활용한 RAG 검색
- 검색된 근거 문서를 바탕으로 추천 답변 생성
- 외부 LLM 연결 실패 시 RAG 템플릿 답변으로 대체

중앙 영역 - 대표 결과:
- 예측 클래스: 수납장, 의자, 냉장고, 테이블, TV
- 추천 기준: 카테고리, 사용 목적, 배치 조건, 사용자 질문
- 결과 화면: 예측 카테고리, confidence, 추천 답변, RAG 근거 문서

오른쪽 영역 - 사용 기술:
- Python, Streamlit
- TensorFlow/Keras, MobileNetV2
- TF-IDF, cosine similarity
- Gemini API, Groq API, Ollama, fallback template

넣을 개념 박스:
- Image Classification: 업로드 이미지를 5개 가구 클래스로 분류
- RAG: 검색된 문서를 근거로 답변 생성
- Fallback: 외부 LLM 장애 시 대체 답변 제공

캡처 후보:
- 필수 자료: Streamlit 대표 결과 화면
  - 캡처 위치: 앱 실행 화면에서 이미지 업로드, 예측 결과, 추천 답변이 한 화면에 보이는 부분
  - 코드 기준: `app.py` 682~725행은 이미지 업로드와 예측 결과 출력 흐름
  - 코드 기준: `app.py` 730~790행은 질문 입력, RAG 검색, 추천 답변, 근거 문서 출력 흐름
- 보조 자료: 이미지 분류 결과 카드 또는 추천 답변 카드
  - 이미지 분류 결과만 강조하려면 예측 카테고리와 confidence 카드만 잘라서 사용
  - 추천 답변을 강조하려면 답변 카드와 source 표시 부분을 잘라서 사용
- 이동/요약 자료: 전체 구조도는 Page 2에서 사용

### Page 2 - 문제 정의와 시스템 구조

목표:
`왜 이렇게 만들었는지`와 `이미지 분류, RAG 검색, LLM 답변이 어떻게 연결되는지`를 설명한다.

상단 제목:
`문제 정의와 시스템 구조`

왼쪽 영역 - 문제 정의:
- 가구 추천은 텍스트 조건뿐 아니라 이미지, 공간, 용도, 배치 조건의 영향을 받는다.
- 사용자의 이미지와 질문을 함께 활용하면 더 구체적인 추천 흐름을 만들 수 있다.
- 검색 근거를 보여주는 RAG 구조로 답변 신뢰도를 높이고자 했다.

중앙 영역 - 전체 흐름:
`이미지 업로드 -> 전처리 -> MobileNetV2 분류 -> 카테고리 추출 -> RAG 문서 검색 -> LLM 답변 생성 -> Streamlit 출력`

오른쪽 영역 - 기술 구성:
- 이미지 분류: MobileNetV2 기반 전이학습
- 입력 크기: 160 x 160 RGB
- 검색: TF-IDF + cosine similarity
- 답변: Gemini/Groq/Ollama/fallback

캡처 후보:
- 필수 자료: 전체 처리 구조도
  - 캡처 위치 1: 기존 산출물 PDF 7페이지 `시스템 전체 구조`
  - 캡처 범위: 1. 이미지 업로드부터 7. Streamlit 결과 출력까지 보이는 구조도 영역
  - 더 좋은 방식: PDF 7페이지를 그대로 붙이지 말고, 새 PPT에서 같은 흐름을 도형으로 다시 그림
- 보조 자료: RAG 지식 문서 일부
  - 캡처 위치: `furniture_knowledge.md`
  - 캡처 범위: `이미지 분류 결과 해석 가이드`, `수납장 추천 가이드`, `의자 추천 가이드` 중 1~2개 섹션
  - 사용 목적: RAG가 아무 문서나 검색하는 것이 아니라 직접 만든 추천 지식 문서를 근거로 답변한다는 점을 보여줌
- 이동/요약 자료: Streamlit 메인 화면은 Page 1 또는 Page 4에서 사용

### Page 3 - RAG 검색과 답변 생성 구현

목표:
RAG가 실제로 어떻게 동작하는지 코드 중심으로 보여준다.

상단 제목:
`RAG 검색과 답변 생성`

왼쪽 영역 - RAG 검색:
- `TfidfVectorizer`로 지식 문서를 벡터화
- 사용자 질문을 벡터로 변환
- `cosine_similarity`로 질문과 문서의 유사도 계산
- 이미지 예측 카테고리와 같은 문서에는 bonus 적용
- Top-K 문서를 답변 근거로 사용

중앙 영역 - LLM 답변:
- 검색된 문서를 context로 구성
- Gemini API 연결 시 외부 LLM 답변 생성
- Groq API를 보조 연결로 사용
- Ollama 로컬 모델도 후보로 두었음

오른쪽 영역 - fallback:
- 외부 LLM 호출 실패 시 앱이 멈추지 않음
- 검색된 RAG 문서 기반 템플릿 답변 제공
- 사용자 화면에는 답변 source와 오류 상태를 표시

캡처 후보:
- 필수 자료: `FurnitureRAG.retrieve()`
  - 캡처 위치: `rag_core.py` 91~102행
  - 캡처 내용: 질문 벡터화, cosine similarity 계산, category bonus, Top-K 반환
  - 캡처 목적: RAG 검색이 어떤 기준으로 근거 문서를 고르는지 보여줌
- 보조 자료: `generate_chat_answer_with_meta()`
  - 캡처 위치: `rag_core.py` 121~166행
  - 캡처 내용: fallback 답변 생성, Ollama 호출 시도, 실패 시 RAG template 반환
  - 캡처 목적: 외부/로컬 LLM 연결 실패 시에도 답변을 제공하는 구조를 보여줌
- 이동/요약 자료: Gemini/Groq 보완 내용
  - 기존 산출물에는 Gemini/Groq 보완 내용이 있으므로 Page 5 트러블슈팅 문장으로 요약
  - 외장하드 현재 코드 캡처는 Ollama/fallback 흐름을 사용

### Page 4 - 시연 화면과 이미지 전처리

목표:
LLM/RAG 프로젝트에서 가장 포트폴리오 가치가 높은 “연결 구조”와 “장애 대응”을 보여준다.

상단 핵심 결과:
- 이미지 분류 결과를 검색 카테고리로 활용
- 사용자 질문 기반 관련 문서 Top-K 검색
- Gemini/Groq/Ollama 중 가능한 응답 경로 사용
- 외부 AI 연결 실패 시 RAG 문서 기반 답변으로 대체

왼쪽 영역 - Streamlit 화면:
- 이미지 업로드 영역
- 예측 카테고리와 확률
- 질문 입력창
- 추천 답변 결과
- RAG 근거 문서 펼침 화면

가운데 영역 - 핵심 코드:
- 이미지 전처리 코드
- direct resize
- square padding
- center crop
- mirror
- multi-view average

오른쪽 영역 - 결과 해석:
- 이미지 분류 결과가 RAG 검색 조건으로 이어진다.
- 사용자는 예측 카테고리와 추천 답변을 한 화면에서 확인할 수 있다.
- RAG 근거 문서를 펼쳐 답변의 출처를 확인할 수 있다.

코드 캡처 후보:
- 필수 자료: Streamlit 시연 화면
  - 캡처 위치: 앱 실행 화면에서 이미지 업로드, 예측 카테고리, 추천 질문, 추천 답변, RAG 근거 문서가 보이는 영역
  - 코드 기준: `app.py` 682~725행은 업로드와 이미지 분류
  - 코드 기준: `app.py` 730~790행은 질문 입력, `rag.retrieve()`, 답변 생성, 근거 문서 표시
- 보조 자료: `FurnitureImagePredictor.preprocess()`
  - 캡처 위치: `image_predictor.py` 80~117행
  - 캡처 내용: direct resize, square padding, center crop, mirror, multi-view average 준비
  - 캡처 목적: 이미지 비율과 배경 차이에 대응하려고 여러 전처리 결과를 함께 사용했다는 점을 보여줌
- 보조 자료: `FurnitureImagePredictor.predict()`
  - 캡처 위치: `image_predictor.py` 119~141행
  - 캡처 내용: 여러 전처리 예측 확률 평균, 최종 class/confidence 반환
  - 공간이 부족하면 `preprocess()`만 넣고 `predict()`는 문장으로 요약
- 이동/요약 자료: Streamlit session_state 처리
  - 캡처 위치: `app.py` 663~674행 또는 715~720행
  - 사용 기준: 이전 예측 결과와 RAG 근거 문서를 화면 상태로 유지했다는 설명이 필요할 때만 문장으로 요약

### Page 5 - 트러블슈팅과 개선 방향

목표:
연동형 AI 프로젝트에서 마주친 모델 호환성과 LLM 연결 문제를 정리한다.

왼쪽 영역 - 트러블슈팅:
1. Keras 모델 로딩 오류
   - 문제: Kaggle에서 학습한 모델이 로컬 Streamlit에서 로딩되지 않음
   - 원인: TensorFlow/Keras 버전 차이와 `quantization_config` 오류
   - 해결: 호환용 `.keras` 모델을 생성하고 Streamlit에서 호환 모델을 우선 로딩
2. Ollama 연동 실패
   - 문제: 로컬 Ollama 서버와 LLM 연동이 반복 실패
   - 원인: 로컬 서버 실행 상태, API 연결, 메모리 환경 문제
   - 해결: Gemini API 연동, Groq 보조 연결, 최종 fallback 답변 구조 추가
3. 이미지 비율에 따른 예측 흔들림
   - 문제: 업로드 이미지 비율과 배경에 따라 예측 확률이 흔들림
   - 해결: direct, square, crop, mirror 전처리 결과를 평균 내는 방식 적용

오른쪽 영역 - 개선 방향:
- LangChain 또는 Vector Store로 RAG 구조 고도화
- 실제 상품 DB와 가격, 재고, 사용자 취향 정보 연결
- OpenAI API 등 안정적인 LLM provider 연동
- PlusHome과 연결해 이미지 기반 상품 추천 기능으로 확장

코드 캡처 후보:
- 필수 자료: `문제-원인-해결` 트러블슈팅 카드
  - 카드 1: Keras 모델 로딩 오류
  - 카드 2: Ollama 연동 실패와 fallback 구조
  - 카드 3: 이미지 비율에 따른 예측 흔들림
- 보조 캡처: `generate_chat_answer_with_meta()`
  - 캡처 위치: `rag_core.py` 121~166행
  - 사용 목적: Ollama 실패 시 RAG template으로 답변하는 fallback 구조를 코드로 보여줌
- 보조 캡처: `FurnitureImagePredictor.preprocess()`
  - 캡처 위치: `image_predictor.py` 80~117행
  - 사용 목적: 이미지 비율 문제를 direct/square/crop/mirror 전처리로 완화한 점을 보여줌
- 캡처 생략 가능: 기존 산출물 PDF 15~16페이지의 트러블슈팅 화면
  - 그대로 캡처하면 새 요약본 디자인과 어울리지 않을 수 있으므로, 내용만 가져와 새 디자인 카드로 다시 작성하는 것을 권장

하단 문구:
`이 프로젝트를 통해 이미지 분류 결과를 검색 조건으로 연결하고, 외부 LLM 장애 상황에서도 사용자가 답변을 받을 수 있는 RAG 기반 fallback 구조를 구현했습니다.`

---

## 홈페이지 버튼 반영안

간략 PDF가 완성되면 `assets` 폴더에 다음 이름으로 저장하는 것을 추천합니다.

- `ml-project-summary.pdf`
- `dl-furniture-summary.pdf`
- `llm-rag-summary.pdf`

홈페이지 프로젝트 카드 버튼:

- Machine Learning: `GitHub` / `Summary PDF` / `Streamlit`
- Deep Learning: `Summary PDF` / `Streamlit` / `Source ZIP`
- LLM/RAG: `Summary PDF` / `Streamlit` / `Source ZIP`

## 코드 캡처 체크리스트

### 머신러닝

- Model A/B 변수 구성 코드
- 회귀/분류 모델 성능표 코드
- Streamlit 예측 결과 화면

### 딥러닝

- CIFAR-100 가구 클래스 필터링 코드
- CNN 모델 정의 코드
- Streamlit 이미지 예측 코드

### LLM/RAG

- RAG 검색 코드
- LLM fallback 코드
- 이미지 전처리 multi-view 코드
